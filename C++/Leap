#include <iostream>
using namespace std;

int main() {

  int year;
  cout << "Enter a year: ";
  cin >> year;
  
  if (not (year%400 or year%100) or not (year%4 or  year%100 ==0 ))
  {
    cout << year << " is a leap year!";
  } else {
    cout << year << " isn't a leap year!";
  }

  return 0;
}
